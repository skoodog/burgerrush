# Asset ledger

| Date | Asset ID | Tool/model | Attempt | Purpose | Result                                          | Estimated credits/cost | File/provenance                                                                                              |
| ---- | -------- | ---------- | ------: | ------- | ----------------------------------------------- | ---------------------: | ------------------------------------------------------------------------------------------------------------ |
| -    | -        | none       |       0 | -       | No external generation was performed for v0.3.0 |                      0 | All art and audio generated procedurally at runtime by `src/game/art/` and `src/game/audio/AudioDirector.ts` |

## Provenance statement

Every texture and every sound in this build is produced by code in this
repository at boot. No image, audio, video or model was fetched, generated by a
third-party service, traced, or derived from any existing game. The supplied
design sheet informed colour, mood and roster choices only.

When external generation is used, log every attempt above before spending
credits, and record the prompt, model, attempt index and resulting file.

## Required revision-2 asset families

- `chef_p_model` / `chef_p_flight`
- `chef_s_model` / `chef_s_flight`
- `sp_sibling_launch` / `sp_dual_fire` / `sp_victory`
- `enemy_onion_ringlets`
- `boss_dread_stack_base` / world variants
- `projectile_pickle` / `projectile_ketchup` / `projectile_mustard` / `projectile_mayo`
- `ui_boss_warning_en` / `ui_boss_warning_ja`
- `sfx_boss_klaxon` / `music_boss_flight`
